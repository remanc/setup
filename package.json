{
  "name": "setup",
  "version": "1.0.0",
  "description": "## Create a new NPM project",
  "main": "index.js",
  "scripts": {
    "test-unit": "istanbul cover --dir reports/coverage _mocha -- --reporter dot",
    "test-coverage": "istanbul check-coverage --statement 100 --branch 100 --function 100 --lines 100",
    "test": "npm run test-unit && npm run test-coverage"
  },
  "repository": {
    "type": "git",
    "url": "git@github-remanc:remanc/setup.git"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "chai": "^3.5.0",
    "coveralls": "^2.11.8",
    "istanbul": "^0.4.2",
    "mocha": "^2.4.5"
  }
}
